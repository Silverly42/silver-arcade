const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const root=path.join(__dirname,'games/slider-io');
test('build identifier is visible and multiplayer controls are disabled',()=>{const version=fs.readFileSync(path.join(root,'version.js'),'utf8'),html=fs.readFileSync(path.join(root,'index.html'),'utf8');assert.match(version,/SLIDER_BUILD='1\.2\.0-beta\.5'/);assert.match(version,/SLIDER_ONLINE=false/);assert.match(html,/id="onlinePanel" class="online hidden"/);assert.match(html,/id="buildHud"/);assert.match(html,/id="buildMenu"/);assert.match(html,/id="hooverBtn"/);assert.match(html,/id="quitBtn"/);assert.match(html,/id="quitPaused"/)});
